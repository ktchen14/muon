#ifndef MU_STATOR_BOOLEAN_SIGN_I
#define MU_STATOR_BOOLEAN_SIGN_I

#include <muon/stator/boolean_sign.h>  // IWYU pragma: export

#include "node.h"

#include <stddef.h>

__attribute__((const, nonnull))
static inline const mu_node_t *boolean_sign_at(
    const mu_boolean_sign_t *sign, size_t i) {
  return NULL;
}

#endif /* MU_STATOR_BOOLEAN_SIGN_I */
